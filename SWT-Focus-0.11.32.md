# SWT Focus 0.11.32

- 重做题库外问题的生成时机：稳定的考官语音片段出现后即提前请求 DeepSeek；完整字幕若确认是同一问题，就直接采用结果，不再等待完整终稿后才开始请求。
- 修复“考官刚开始下一段声音就取消上一题模型请求”的问题；只有完成识别并确认是不同新问题时，旧请求才会取消。
- 把未知题模型请求压缩为两套回答和中译必需字段，单题输出预算由至少 320 缩为 260，超时由 4.5 秒缩为 3.2 秒；题库题仍完全本地秒出。
- 彻底移除“simple and honest / stay open-minded”这类答非所问的万能回答。模型不可用或问题无法可靠判断时，改为礼貌请考官重复，避免编造或乱答。
- 新增“英语不够好、到美国后如何应对”这一类的本地适应模板，作为模型慢或不可用时的贴题安全保障。

# SWT Focus 0.11.31

- 修复电话转写里“自我介绍”被重复识别的情况：`brief intro` 与 `brief introduction about yourself` 现在视为同一题，只保留信息完整的一次。
- 修复 `And what's your name?` 被黏到上一问的问题；自我介绍、姓名、年龄会分别识别、按顺序合并回答。
- 修复多题回答漏掉中文翻译的问题：中文答案现在逐项保留，不会因错误去重而只显示第一句。
- 年龄未填写时不再把“请在设置中填写”这种内部提示塞进英文答案；会显示简洁的 `(your age)` 供你在设置保存真实年龄后自动替换。
- 新增上述截图对应的完整流式回归：短版 ASR 终稿 + 完整终稿、重复自我介绍、姓名和年龄三问均已验证。

# SWT Focus 0.11.30

- 修复通话开场的连续问题："Could you introduce yourself? / Can you hear me? / Where are you?" 会逐题显示对应回答，不再落入题外万能答案。
- 修复没有识别出问号的连续提问：识别到新的大写问句开头时，会安全拆成多题并按顺序合并答案。
- 修复岗位问题被截断时的误匹配："What kind of job do you want to apply ..." 会等待后半句，拼完整后匹配赴美岗位模板。
- 按住空格开始回答即锁定本轮问题；松开后才接收下一问，避免考官新问题或迟到字幕混入你正在回答的上下文。
- 新增上述三类回归检查；题库匹配、断句拼接、141 组真实问法与 60 组审校题均已通过本地验证。

# SWT Focus 0.11.29

- 重构多问题处理：一段话中的多个问题会按顺序拆开，模板 1 与模板 2 都覆盖每一题。
- 追问会优先沿用你刚刚说出的事实；例如回答 swimming 后，“Why do you like it?” 不会变成 basketball。
- 题库内的审校模板不再被网络模型改写；DeepSeek V4-Flash 只在题库外问题或题库外子题上后台补全。
- DeepSeek 请求加入短超时与轮次校验。考官开始下一问时，旧请求立即作废，不能再覆盖新题。
- 已加入长问题、两题/三题、连续追问、学生尾音迟到等运行时回归测试。

# SWT Focus 0.11.28

- 完成应用内更新的真实回归验证：下载、签名校验、退出旧版、替换应用和自动重启会作为一条完整流程执行。

# SWT Focus 0.11.27

- 修复已验证更新包在“练习设置”窗口内等待应用退出的问题：先正常关闭窗口；若 AppKit 没有结束进程，更新动作会在已完成安全校验后结束旧进程，让替换和重启继续执行。

# SWT Focus 0.11.26

- 修复长问题被直接忽略的缺陷：正常的长场景题不再受 60 词限制而丢失；带有长前言的录音会优先提取真正的问题句。
- 识别到稳定的问题核心后会先给出本地可读模板；考官仍在补充时，界面只先显示一份临时模板，最终字幕到达后再精确更新。
- DeepSeek 继续只在后台补全个性化内容，第一份模板不再等待网络请求。
- 扩展了“客户很生气 / 遇到问题时如何处理”等长场景题的本地识别，立即显示贴题的 customer-service 模板。

# SWT Focus 0.11.25

- 修复“发现云端更新但点击安装后失败”：改为免费本机更新流程。下载包会先校验签名、应用身份和代码签名，验证通过才自动重启替换。
- 设置页的按钮改为“下载并安装”，会直接显示下载、校验或失败状态，不再出现两层意思不清的安装按钮。

# SWT Focus 0.11.24

本次更新把所有实时口测统一为“按住作答”：

- 松开空格时只把考官声道送去问题识别；按住空格时只把你的麦克风送去回答识别。
- 手机免提和 Mac 通话 / 录音都使用同一套操作，不再需要理解四种容易混淆的声音模式。
- 松开后保留约 0.3 秒回答尾音，避免最后一个单词被截断；窗口失去焦点或停止监听时自动复位。
- 两条转写连接始终保持待机，切换角色不需要重新联网，也不会因为网络延迟把上一角色的终稿丢掉。
- 你的真实回答会写入当前轮上下文；例如回答 swimming 后，后续 “Why do you like that sport?” 的 A/B 两套模板都会继续使用 swimming。
- 底部状态条明确显示“正在听考官 / 正在听你的回答 / 正在收尾”，也可以直接用鼠标按住。

双回答模板、中文逐句翻译、随机题 DeepSeek 分析和答案一致性检查继续保留。
